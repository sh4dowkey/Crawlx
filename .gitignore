# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when run with the
# -o option to specify a file path.
*.out

# Go workspace file
go.work
go.work.sum

# Compiled binaries folder
/dist/